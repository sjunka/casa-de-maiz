defaults.url=https://sentry.io/
defaults.org=REPLACE_WITH_SENTRY_ORG_SLUG
defaults.project=REPLACE_WITH_SENTRY_PROJECT_SLUG
# auth.token intentionally omitted — set SENTRY_AUTH_TOKEN in the build environment instead.
